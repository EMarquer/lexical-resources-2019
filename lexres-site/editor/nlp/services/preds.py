from ...utils.models import get_language_model
"""
    This script should contain only functions for predicting the next word.
    Functions should all share the same signature.
"""
def pred_foo_function(text=""):
    """
        Example function. To be removed
    """
    return get_language_model()(text)#["a", "list", "of", "possible", "next", "words"]
